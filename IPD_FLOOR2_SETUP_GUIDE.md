# คู่มือการติดตั้งฐานข้อมูล IPD ชั้น 2 แยกตามเลขเตียง

## 📋 ภาพรวม

ระบบนี้จะบันทึกข้อมูลผู้ป่วยหอผู้ป่วยในชั้น 2 ไปยัง **Google Sheets ไฟล์เดียวกันกับฐานข้อมูลเลื่อนนัด Admit** โดยแยกข้อมูลตามเลขเตียงเป็น Sheet ย่อยต่างๆ

### ตัวอย่าง:
- **Sheet1**: ตาราง 1 (ฐานข้อมูลเลื่อนนัด Admit - เดิม)
- **B4**: ข้อมูลผู้ป่วยเตียง B4
- **B5**: ข้อมูลผู้ป่วยเตียง B5
- **V1**: ข้อมูลผู้ป่วยเตียง V1
- **V2**: ข้อมูลผู้ป่วยเตียง V2
- ... และอื่นๆ

---

## 🚀 ขั้นตอนการติดตั้ง

### 1️⃣ อัปเดต Google Apps Script

1. เปิด Google Sheets ที่มีฐานข้อมูลเลื่อนนัด Admit
2. ไปที่ **Extensions** > **Apps Script**
3. **ลบโค้ดเดิมทั้งหมด**
4. คัดลอกโค้ดจากไฟล์ **`google-apps-script-ipd-floor2.js`** ทั้งหมด
5. วางในหน้า Apps Script Editor
6. กด **Save** (Cmd+S)
7. **Deploy ใหม่**:
   - คลิก **Deploy** > **Manage deployments**
   - คลิกไอคอนดินสอ ✏️
   - เลือก **New version**
   - คลิก **Deploy**

### 2️⃣ เพิ่มไฟล์ JavaScript ใหม่ในโปรเจค

1. เปิดไฟล์ **`google-sheet-config.js`**
2. เพิ่มโค้ดจากไฟล์ **`google-sheet-config-ipd-floor2.js`** ต่อท้ายโค้ดเดิม
3. บันทึกไฟล์

### 3️⃣ เพิ่ม Script Tag ในหน้า IPD

เปิดไฟล์ **`ipd.html`** และเพิ่ม script tag ก่อน `</body>`:

```html
<script src="google-sheet-config.js"></script>
<script src="ipd-functions.js"></script>
```

### 4️⃣ แก้ไข ipd-functions.js เพื่อเรียกใช้งาน

เพิ่มโค้ดส่งข้อมูลไป Google Sheets ในฟังก์ชันที่เกี่ยวข้อง:

#### ✅ เมื่อ Admit ผู้ป่วยเข้าเตียง:
```javascript
// ในฟังก์ชัน confirmAdmitToBed() หรือฟังก์ชันที่ Admit ผู้ป่วยเข้าเตียง
if (typeof sendIPDFloor2DataToGoogleSheet === 'function' && isGoogleSheetConfigured()) {
  sendIPDFloor2DataToGoogleSheet({
    bed_number: bedNumber,
    patient_hn: patient.patient_hn,
    patient_name: patient.patient_name,
    age: patient.age,
    gender: patient.gender,
    diagnosis: patient.diagnosis,
    doctor: patient.doctor,
    admit_date: patient.admitted_date || new Date().toISOString(),
    discharge_date: null,
    status: 'Admitted',
    imc_status: patient.imc_status,
    notes: 'Admitted to bed ' + bedNumber
  }).catch(error => {
    console.error('เกิดข้อผิดพลาดในการส่งข้อมูลไป Google Sheets:', error);
  });
}
```

#### ✅ เมื่อจำหน่ายผู้ป่วย:
```javascript
// ในฟังก์ชัน confirmDischarge() หรือฟังก์ชันที่จำหน่ายผู้ป่วย
if (typeof sendIPDFloor2DataToGoogleSheet === 'function' && isGoogleSheetConfigured()) {
  sendIPDFloor2DataToGoogleSheet({
    bed_number: patient.bed_number,
    patient_hn: patient.patient_hn,
    patient_name: patient.patient_name,
    age: patient.age,
    gender: patient.gender,
    diagnosis: patient.diagnosis,
    doctor: patient.doctor,
    admit_date: patient.admitted_date,
    discharge_date: new Date().toISOString(),
    status: 'Discharged',
    imc_status: patient.imc_status,
    notes: dischargeReason // เหตุผลการจำหน่าย
  }).catch(error => {
    console.error('เกิดข้อผิดพลาดในการส่งข้อมูลไป Google Sheets:', error);
  });
}
```

#### ✅ เมื่อ Refer ผู้ป่วย:
```javascript
// ในฟังก์ชัน confirmRefer() หรือฟังก์ชันที่ Refer ผู้ป่วย
if (typeof sendIPDFloor2DataToGoogleSheet === 'function' && isGoogleSheetConfigured()) {
  sendIPDFloor2DataToGoogleSheet({
    bed_number: patient.bed_number,
    patient_hn: patient.patient_hn,
    patient_name: patient.patient_name,
    age: patient.age,
    gender: patient.gender,
    diagnosis: patient.diagnosis,
    doctor: patient.doctor,
    admit_date: patient.admitted_date,
    discharge_date: new Date().toISOString(),
    status: 'Referred',
    imc_status: patient.imc_status,
    notes: 'Referred: ' + referReason // เหตุผลการ Refer
  }).catch(error => {
    console.error('เกิดข้อผิดพลาดในการส่งข้อมูลไป Google Sheets:', error);
  });
}
```

---

## 📊 โครงสร้างข้อมูลในแต่ละ Sheet (แยกตามเตียง)

แต่ละ Sheet จะมีคอลัมน์ดังนี้:

| คอลัมน์ | ข้อมูล |
|---------|--------|
| **วันที่บันทึก** | วันที่บันทึกข้อมูล (พ.ศ.) |
| **เวลาบันทึก** | เวลาที่บันทึกข้อมูล |
| **HN** | Hospital Number |
| **ชื่อผู้ป่วย** | ชื่อ-นามสกุล |
| **อายุ** | อายุผู้ป่วย |
| **เพศ** | เพศ |
| **Diagnosis** | การวินิจฉัยโรค |
| **แพทย์** | แพทย์ผู้รักษา |
| **วันที่ Admit** | วันที่รับ Admit เข้าเตียง |
| **วันที่จำหน่าย** | วันที่จำหน่าย/Refer |
| **สถานะ** | Admitted / Discharged / Referred |
| **IMC/Non-IMC** | ประเภทผู้ป่วย |
| **หมายเหตุ** | หมายเหตุเพิ่มเติม |
| **Timestamp** | เวลาแบบ ISO format |

---

## 🎯 ตัวอย่างการใช้งาน

### ผู้ป่วยเข้าเตียง B4:
- ระบบจะสร้าง Sheet ชื่อ **"B4"** อัตโนมัติ (ถ้ายังไม่มี)
- บันทึกข้อมูลการ Admit ลงในแถวใหม่

### ผู้ป่วยจำหน่ายจากเตียง V1:
- ระบบจะบันทึกข้อมูลการจำหน่ายลงใน Sheet **"V1"**
- พร้อมระบุวันที่จำหน่ายและสถานะ

### ดูประวัติเตียง B5:
- เปิด Sheet **"B5"**
- จะเห็นประวัติผู้ป่วยทุกคนที่เคยใช้เตียงนี้เรียงตามเวลา

---

## 🔍 การตรวจสอบและแก้ไขปัญหา

### ✅ ตรวจสอบว่าระบบทำงานหรือไม่

1. เปิด Console ในเบราว์เซอร์ (F12 > Console)
2. Admit ผู้ป่วยเข้าเตียง
3. ดูข้อความ:
   - ✅ `🔄 กำลังส่งข้อมูล IPD Floor 2 ไป Google Sheets...`
   - ✅ `✅ ส่งข้อมูลเตียง B4 ไป Google Sheets สำเร็จ`

### ❌ หากเกิดปัญหา

**ปัญหา: Sheet ไม่ถูกสร้าง**
- ✔️ ตรวจสอบว่า Deploy Google Apps Script แล้ว
- ✔️ ตรวจสอบว่า URL ถูกต้อง
- ✔️ ตรวจสอบว่าโค้ดมี `data_type: 'ipd_floor2'` ใน request

**ปัญหา: ข้อมูลไม่ถูกบันทึก**
- ✔️ ตรวจสอบว่าเรียกฟังก์ชัน `sendIPDFloor2DataToGoogleSheet()` แล้ว
- ✔️ ตรวจสอบว่ามี `bed_number` ในข้อมูล
- ✔️ ดู Console log เพื่อดู error message

**ปัญหา: ข้อมูล Postpone Admit หายไป**
- ✅ ไม่ต้องกังวล! ข้อมูลเลื่อนนัดยังอยู่ใน Sheet1 เหมือนเดิม
- ✅ ระบบใหม่จะแยกเฉพาะข้อมูล IPD Floor 2 ไปเป็น Sheet ต่างหาก

---

## 💡 ข้อดีของระบบนี้

✅ **แยกข้อมูลตามเตียง** - ง่ายต่อการค้นหาและวิเคราะห์
✅ **ใช้ไฟล์เดียว** - ไม่ต้องจัดการหลายไฟล์
✅ **สร้าง Sheet อัตโนมัติ** - ไม่ต้องสร้างล่วงหน้า
✅ **บันทึกประวัติครบถ้วน** - เห็นผู้ป่วยทุกคนที่ผ่านเตียงนั้นๆ
✅ **รองรับทั้ง Admit, Discharge, Refer** - บันทึกทุก Event
✅ **ไม่กระทบระบบเดิม** - ข้อมูล Postpone Admit ยังทำงานปกติ

---

## 📝 หมายเหตุ

- ระบบจะบันทึกข้อมูลทุกครั้งที่มีการ Admit, จำหน่าย, หรือ Refer ผู้ป่วย
- แต่ละเตียงจะมี Sheet แยกกัน ทำให้ง่ายต่อการติดตามและวิเคราะห์
- สามารถ Export แต่ละ Sheet เป็น Excel ได้อิสระ
- ข้อมูลทั้งหมดเก็บในรูปแบบ Thai Buddhist Calendar (พ.ศ.)

---

✅ **ระบบพร้อมใช้งาน!** 🎉
