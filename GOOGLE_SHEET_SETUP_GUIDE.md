# คู่มือการติดตั้งฐานข้อมูล Google Sheets สำหรับการเลื่อนนัด Admit

## 📋 ขั้นตอนการติดตั้ง

### 1️⃣ สร้าง Google Sheets

1. เปิด [Google Sheets](https://sheets.google.com)
2. คลิก **+ Blank** เพื่อสร้างไฟล์ใหม่
3. ตั้งชื่อไฟล์ว่า **"ฐานข้อมูลเลื่อนนัด Admit - Smart Bed Planner"**

### 2️⃣ ติดตั้ง Google Apps Script

1. ในหน้า Google Sheets ที่สร้างไว้ ไปที่เมนู **Extensions** > **Apps Script**
2. จะเปิดหน้าต่างใหม่ขึ้นมา ให้ลบโค้ดเดิมออกทั้งหมด
3. เปิดไฟล์ `google-apps-script-postpone.js` ในโฟลเดอร์โปรเจค
4. คัดลอกโค้ดทั้งหมดแล้ววางในหน้าต่าง Apps Script
5. คลิก **💾 Save** (หรือกด Ctrl+S / Cmd+S)
6. ตั้งชื่อโปรเจคว่า **"Postpone Admit API"**

### 3️⃣ Deploy Web App

1. คลิกปุ่ม **Deploy** (ด้านบนขวา) > เลือก **New deployment**
2. คลิกไอคอน ⚙️ (ด้านซ้าย) > เลือก **Web app**
3. ตั้งค่าดังนี้:
   - **Description**: "Postpone Admit Database v1.0"
   - **Execute as**: **Me** (อีเมลของคุณ)
   - **Who has access**: **Anyone**
4. คลิก **Deploy**
5. อาจต้องยืนยันสิทธิ์:
   - คลิก **Authorize access**
   - เลือกบัญชี Google ของคุณ
   - คลิก **Advanced** > **Go to Postpone Admit API (unsafe)**
   - คลิก **Allow**
6. **สำคัญ**: คัดลอก **Web app URL** ที่ได้ (จะมีรูปแบบคล้าย `https://script.google.com/macros/s/ABC...XYZ/exec`)

### 4️⃣ ตั้งค่า URL ในระบบ

1. เปิดไฟล์ `google-sheet-config.js`
2. แทนที่ `YOUR_SCRIPT_ID` ด้วย URL ที่คัดลอกมา:
   ```javascript
   postponeAdmitURL: 'https://script.google.com/macros/s/ABC...XYZ/exec',
   ```
3. บันทึกไฟล์

### 5️⃣ ทดสอบระบบ

1. เปิดหน้า `registry.html` ในเบราว์เซอร์
2. เลือกผู้ป่วยที่ยืนยันการจองแล้ว
3. คลิกปุ่ม **"⏰ เลื่อนวัน"**
4. กรอกวันที่ใหม่และเหตุผล
5. คลิก **"✓ ยืนยันเลื่อนวัน"**
6. กลับไปตรวจสอบที่ Google Sheets จะต้องเห็นข้อมูลที่บันทึก

---

## 📊 โครงสร้างข้อมูลใน Google Sheets

ระบบจะสร้างตารางพร้อมคอลัมน์ดังนี้:

| คอลัมน์ | ข้อมูล |
|---------|--------|
| **วันที่บันทึก** | วันที่ที่บันทึกข้อมูล (พ.ศ.) |
| **เวลาบันทึก** | เวลาที่บันทึกข้อมูล |
| **HN** | เลข Hospital Number ของผู้ป่วย |
| **ชื่อผู้ป่วย** | ชื่อ-นามสกุลผู้ป่วย |
| **อายุ** | อายุผู้ป่วย |
| **เพศ** | เพศผู้ป่วย |
| **โรค** | การวินิจฉัยโรค (Diagnosis) |
| **แพทย์** | แพทย์ผู้รักษา |
| **วันที่นัดเดิม** | วันที่นัดเดิมก่อนเลื่อน |
| **วันที่นัดใหม่** | วันที่นัดใหม่หลังเลื่อน |
| **เหตุผลการเลื่อน** | เหตุผลที่เลื่อนวัน |
| **ผู้บันทึก** | ชื่อผู้ใช้งานที่ทำการเลื่อนวัน |
| **IMC/Non-IMC** | ประเภทผู้ป่วย |
| **สถานะการชำระเงิน** | สถานะการชำระเงินล่วงหน้า |
| **เตียงที่จอง** | หมายเลขเตียงที่จอง |
| **Timestamp** | เวลาแบบ ISO format สำหรับอ้างอิง |

---

## 🔍 การตรวจสอบและแก้ไขปัญหา

### ✅ ตรวจสอบว่าระบบทำงานหรือไม่

1. เปิด Console ในเบราว์เซอร์ (F12 > Console)
2. ดูข้อความขณะกดปุ่มเลื่อนวัน:
   - ✅ `🔄 กำลังส่งข้อมูลไป Google Sheets...`
   - ✅ `✅ ส่งข้อมูลไป Google Sheets สำเร็จ`

### ❌ หากเกิดปัญหา

**ปัญหา: ข้อมูลไม่ถูกบันทึกใน Google Sheets**
- ✔️ ตรวจสอบว่า URL ถูกต้องหรือไม่
- ✔️ ตรวจสอบว่าได้ Deploy Web App แล้ว
- ✔️ ตรวจสอบว่าตั้งค่า "Who has access" เป็น "Anyone"
- ✔️ ลองเปิด URL ที่ Deploy ได้ในเบราว์เซอร์ ควรแสดง `{"status":"ok","message":"Postpone Admit API is running"}`

**ปัญหา: Google Sheets แสดง Error**
- ✔️ ตรวจสอบว่าคัดลอกโค้ดครบถ้วน
- ✔️ ตรวจสอบว่า Google Sheets ไม่ถูกเปิดโดยหลายคนพร้อมกัน
- ✔️ ลองเปิด Apps Script Editor > กด Run > เลือก doPost > ดู error log

**ปัญหา: Console แสดง "CORS error"**
- ✅ นี่เป็นเรื่องปกติ เพราะเราใช้ mode: 'no-cors'
- ✅ ข้อมูลจะถูกส่งไปแม้จะมี error นี้

---

## 🔐 ความปลอดภัยของข้อมูล

- ✅ ข้อมูลถูกส่งผ่าน HTTPS
- ✅ Google Apps Script ทำงานภายใต้บัญชี Google ของคุณเอง
- ✅ เฉพาะคุณเท่านั้นที่เห็นและจัดการข้อมูลได้
- ✅ ระบบไม่เก็บข้อมูลผู้ป่วยที่ไหนนอกจาก Google Sheets ของคุณ

---

## 📝 ไฟล์ที่เกี่ยวข้อง

1. **google-apps-script-postpone.js** - โค้ดสำหรับ Google Apps Script (วางใน Apps Script Editor)
2. **google-sheet-config.js** - ไฟล์ตั้งค่า URL (แก้ไข URL แล้วเก็บไว้ในโฟลเดอร์โปรเจค)
3. **registry-functions.js** - ไฟล์ระบบหลัก (ถูกแก้ไขให้ส่งข้อมูลไป Google Sheets อัตโนมัติ)
4. **registry.html** - หน้าทะเบียนจองเตียง (เพิ่ม script tag สำหรับ google-sheet-config.js)

---

## 💡 Tips

- 📊 คุณสามารถสร้าง Chart และ Dashboard ใน Google Sheets เพื่อวิเคราะห์ข้อมูลการเลื่อนนัดได้
- 🔄 หากต้องการอัปเดตโค้ด ให้ทำ Deploy ใหม่ (New deployment) แล้วเปลี่ยน URL
- 📱 Google Sheets สามารถเปิดดูข้อมูลผ่านมือถือได้ทันที
- 📤 สามารถ Export ข้อมูลเป็น Excel หรือ PDF ได้

---

## 🆘 ติดต่อขอความช่วยเหลือ

หากมีปัญหาหรือข้อสงสัย สามารถตรวจสอบ Console log หรือตรวจสอบ Apps Script Execution log ได้

✅ **ระบบพร้อมใช้งาน!**
